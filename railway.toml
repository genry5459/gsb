[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python gold_micro_scalper_unified.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 300
